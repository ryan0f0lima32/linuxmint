1  sudo apt install curl
    2  sudo curl -fsSLo /usr/share/keyrings/brave-browser-archive-keyring.gpg https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
    3
    4  sudo apt install curl
    5  sudo curl -fsSLo /usr/share/keyrings/brave-browser-archive-keyring.gpg https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
    6  sudo curl -fsSLo /etc/apt/sources.list.d/brave-browser-release.sources https://brave-browser-apt-release.s3.brave.com/brave-browser.sources
    7  sudo apt update
    8  sudo apt install brave-browser
    9  chromium
   10  torbrowser
   11  tor
   12  sudo apt install torbrowser-launcher
   13  sudo apt update
   14  sudo apt install git vim notepadqq 
   15  sudo init 0
   16  sudo
   17  sudo -i
   18  sudo apt update
   19  sudo apt upgrade
   20  sudo apt dist-upgrade
   21  sudo apt full-upgrade
   22  sudo apt autoremove
   23  sudo apt autoclean
   24  sudo apt clean
   25  sudo apt update
   26  sudo apt intall git
   27  sudo apt intall git vim pip python
   28  sudo apt clean
   29  sudo apt update
   30  sudo apt intal 
   31  sudo apt install
   32  sudo apt install git vim python3 pip
   33  https://notepadqq.com/
   34  sudo apt install git vim notepadqq
   35  git cinfig --global user.name "Ryan Ferreira"
   36  git config --global user.name "Ryan Ferreira"
   37  git config --global user global user.email ryan.flima32@gmail.com
   38  git config --global user.email ryan.flima32@gmail.com
   39  git config --global core.editor vim
   40  git config --global merge.tool vimdiff
   41  git config --global color.ui true
   42  git config --list
   43  git pull
   44  git add .
   45  git commit -m "Atualização"
   46  git status
   47  git push 
   48  history